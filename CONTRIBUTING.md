# 参与 DSH Agent RP

欢迎一起完善角色卡迁移与下一代 Agent RP。小而明确的改动最好合并；不需要先写长篇设计文档。

## 报告兼容问题

请在 Issue 中提供：

- DSH 版本与本仓库 commit；
- 卡片格式（PNG、JSON、CHARX、JSONL 或预设 JSON）；
- 涉及的能力，例如开场、世界书、显示正则、MVU 或预设；
- 最短复现步骤、预期表现与实际表现；
- 能公开的最小样本，或去除私人设定后的结构片段。

请勿提交 NPM Token、API Key、私有 DSH 源码、完整 Session Log、付费或无权再分发的角色卡。仅有截图也可以先开 Issue，我们可以一起缩小问题。

## 提交代码

1. 从 `main` 创建分支。
2. 保持改动聚焦，并为用户可见的变化附上截图或简短说明。
3. 至少运行：

   ```powershell
   pnpm run typecheck
   pnpm run build
   ```

4. 如果改动触及导入、提示组装或持久状态，再运行相关的聚焦测试；不要求为了一个局部改动反复跑整套门禁。
5. 向 `main` 提交 Pull Request，说明用户会看到什么，以及你实际检查过什么。

## 适合贡献的方向

- 更多 Character Card / CHARX 资源类型；
- SillyTavern 预设、世界书与显示正则兼容；
- 轻前端的自适应布局和无障碍体验；
- 角色库、Persona、开场与回复版本交互；
- 可公开、可再分发的最小兼容样本；
- 群聊与多角色会话的下一阶段设计。

提交即表示你有权贡献相关内容，并同意其按本项目的 [MIT License](LICENSE) 发布。
